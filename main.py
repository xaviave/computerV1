import sys

from display import display
from computer import compute
from parser import parse_polynomial

if __name__ == "__main__":
    if 
    for argv in sys.argv[1:]:
        parse_polynomial(argv)
        compute()
        display()
